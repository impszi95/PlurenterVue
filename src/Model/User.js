export default class User {
    constructor(username, password, type, terms) {
      this.username = username;
      this.password = password;
      this.type = type;
      this.terms = terms;
    }
  }